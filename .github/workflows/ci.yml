name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GODOT_VERSION: 4.6.1
  GODOT_STATUS: stable

jobs:
  test:
    name: Parse-check + GUT
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Godot binary
        id: cache-godot
        uses: actions/cache@v4
        with:
          path: ~/godot-bin
          key: godot-${{ env.GODOT_VERSION }}-${{ env.GODOT_STATUS }}-linux-x86_64

      - name: Download Godot
        if: steps.cache-godot.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          mkdir -p ~/godot-bin
          curl -fsSL -o /tmp/godot.zip \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-${GODOT_STATUS}/Godot_v${GODOT_VERSION}-${GODOT_STATUS}_linux.x86_64.zip"
          unzip -o /tmp/godot.zip -d /tmp/godot-extracted
          mv /tmp/godot-extracted/Godot_v*_linux.x86_64 ~/godot-bin/godot
          chmod +x ~/godot-bin/godot

      - name: Add Godot to PATH
        run: echo "$HOME/godot-bin" >> "$GITHUB_PATH"

      - name: Godot version
        run: godot --version

      - name: Import project (generates .godot/ on first run)
        run: |
          set -euo pipefail
          # First headless run imports resources. The --quit-after flag exits
          # cleanly after N frames; we tolerate non-zero here because Godot
          # sometimes exits 1 after a clean import on CI.
          godot --headless --import || true

      - name: Parse check
        run: godot --headless --quit

      - name: Main scene smoke-run (60 frames headless)
        run: godot --headless --quit-after 60 res://scenes/Main.tscn

      - name: Title screen smoke-run (60 frames headless)
        # Title screen is now the project's main_scene; this exercises the
        # BuildInfo label load + the "press any key" wait path without
        # actually transitioning into Main.tscn (--quit-after 60 stops
        # before the 0.5s min-visible window elapses).
        run: godot --headless --quit-after 60 res://scenes/TitleScreen.tscn

      - name: Validate bundled puzzles
        run: |
          set -euo pipefail
          shopt -s nullglob
          PUZZLE_FILES=(data/puzzles/*.json)
          if [ ${#PUZZLE_FILES[@]} -eq 0 ]; then
            echo "No puzzle files to validate."
            exit 0
          fi
          # Pass all files in one godot invocation; the tool walks the list.
          godot --headless -s res://tools/puzzle_validate.gd -- "${PUZZLE_FILES[@]/#/res://}"

      - name: Run GUT tests
        run: godot --headless -s res://addons/gut/gut_cmdln.gd -gdir=res://tests/ -gexit
